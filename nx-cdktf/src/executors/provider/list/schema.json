{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "cdktf provider list",
  "description": "List installed providers",
  "type": "object",
  "properties": {
    "entry": {
      "type": "string",
      "default": "main.ts",
      "description": "The path of the entrypoint file (relative to the project root)."
    },
    "json": {
      "type": "boolean",
      "default": false,
      "description": "Get providers list as json"
    }
  },
  "required": []
}
